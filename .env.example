# LLM
LLM_PROVIDER=openai
OPENAI_API_KEY=
GEMINI_API_KEY=

# Infra — 도커 컴포즈로 띄우면 컨테이너 내부 값이 자동 주입되므로 수정 불필요.
# api를 로컬(도커 밖)에서 직접 실행할 때만 사용됨.
DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/jobsim
REDIS_URL=redis://localhost:6379/0
STORAGE_DIR=storage
