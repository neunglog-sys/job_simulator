# 맵 전면 리메이크 — 정의·이미지 프롬프트·Tiled 규격 v2 (2026-07-19)

> 작성: 태수 · 대상: 맵 제작(태수)·배경 생성(콘텐츠)·팀장 확인용
> 기존 맵 45장을 새로 만드는 기준 문서. **왜(§1) → 배경 이미지 프롬프트(§2) → Tiled 작업 규격(§3) → 수용 기준(§4) → 역할(§5)**

---

## 1. 왜 새로 만드나 — 요구사항 정의

### R1. 전역 도달성 (팀장 지시)
- 지금까지는 "막힌 방을 피해서 NPC를 배치"했음 → **거꾸로였다.** NPC가 팀장실 등 **방 안에 있어야 하는 상황이 있고**, 맵이 어디든 갈 수 있어야 배치가 자유로워진다.
- **새 기준: 맵의 모든 방·구역은 플레이어가 걸어서 도달 가능해야 한다.** 막힌 방 자체가 존재하면 안 됨 (도달률 100% 지향 — 기존 맵은 3%~100%로 들쭉날쭉했음).

### R2. 깊이감 — 가구 뒤로 몸이 반쯤 가려지기
- 캐릭터가 책상 옆/뒤를 지날 때 **하반신이 가구에 가려져야** 공간에 "서 있는" 느낌이 남. 현재는 배경 1장 위에 스프라이트를 얹기만 해서 캐릭터가 항상 가구 위에 떠 보임.
- 구현 방식(결정): **배경 PNG는 1장 유지**하고, Tiled에 "가구 상단부" 사각형(overhead)을 그리면 프론트가 **같은 배경에서 그 영역을 잘라 캐릭터 위에 겹쳐 그림** → 뒤에 서면 가려지고 앞에 서면 안 가려짐(y-정렬). **배경 2장 만들 필요 없음** — 단 §3의 레이어 규격과 collision 그리는 법이 바뀜.

### R3. NPC 자리 수를 시나리오에 맞게 (기존 팀 결정)
- 기존: 전 맵 일괄 3자리(teamjang/sasu/bujang) ↔ 미션 등장 NPC는 최대 5명 → 자리 부족 21개 시나리오.
- **새 기준: 그 시나리오의 미션 등장 NPC 수만큼 자리를 찍는다** (필요 수는 태수가 시나리오별 표로 제공).

### R4. 이동 규격 (기존 실측 반영)
- 통로·문 폭은 캐릭터 발판(46px)이 지나가야 함 → **최소 2칸(48px), 권장 3칸(72px)**.

---

## 2. 배경 이미지 생성 프롬프트 (GPT/이미지 모델용)

> 사용법: `{WORKPLACE}`에 직무 공간 설명을 넣어 생성. 영어가 이미지 모델 성능이 좋아 영어 본문 + 아래 한국어 해설.
> 생성 후 §4 체크리스트로 합격 판정하고 나서 Tiled 작업 시작 (불합격이면 재생성이 싸다 — 좌표 작업 후 재생성이 제일 비쌈).

```
Top-down 2.5D view of a {WORKPLACE}, game map background, 1920x1080.

LAYOUT RULES (critical):
- Every room and area MUST be reachable on foot: each room has at least one
  wide open doorway (no closed doors, no fully enclosed rooms).
- One continuous main corridor or open floor connects ALL rooms.
- Walkways and doorways are wide (at least as wide as a desk).
- Furniture is placed against walls or in clear clusters; the center of each
  room and all corridors stay open and walkable.
- No stairs, no ledges, no water, no balconies, no second floor
  (nothing that implies an unreachable or elevated area).

STYLE RULES:
- Clean, warm, detailed illustration style, consistent top-down camera angle
  across the whole image (slight 3/4 tilt so furniture fronts are visible).
- Floor texture clearly distinct from furniture and walls (for easy tracing).
- Even, soft lighting; no dramatic shadows that look like obstacles.
- No people, no characters, no text, no UI elements in the image.
- Desks, shelves and machines drawn as solid objects with a clear visible
  base (bottom edge) where they meet the floor.
```

**`{WORKPLACE}` 예시** (시나리오 미션 내용 기준으로 작성 — 이름 유사도로 하지 말 것, 기존 매핑 원칙):
- ms-06: `modern livestock barn interior with feeding stations, pens along the walls, a small office corner and a supply room`
- yg-03: `modern software development studio with desk clusters, a meeting room, a server corner and a lounge`

**한국어 해설 (왜 이 조항들인가):**
| 조항 | 막으려는 실패 |
|---|---|
| 모든 방에 열린 출입구 | R1 — 닫힌 문·막힌 방이 그림에 있으면 좌표로 못 살림 |
| 통로·문 넓게 | R4 — 46px 발판이 못 지나가는 그림 |
| 계단·단차·물 금지 | "가면 안 될 것 같은" 구역 = 도달성 애매해짐 |
| 가구는 벽쪽·클러스터 | 중앙이 뚫려야 도달률·동선이 나옴 |
| 가구 밑변이 또렷하게 | R2 — overhead/collision 경계(가구가 바닥에 닿는 선)를 따야 함 |
| 사람 금지 | NPC는 우리가 그림 위에 그림 |
| 시점 일관 | 시점이 섞이면 collision을 어디에 그릴지 애매해짐 |

---

## 3. Tiled 작업 규격 v2 — 레이어 고도화

> 기존: `walkable / collision / spawns` 3개. **신규: `overhead` 추가 + collision 그리는 법 변경.**
> (컨버터 tiled-to-geometry.py의 overhead 지원과 프론트 y-정렬 렌더는 태수가 구현 — §5)

| 레이어 | 그리는 것 | v1과 달라진 점 |
|---|---|---|
| `walkable` | 걸을 수 있는 바닥 전체 (사각형 1개~수 개) | 동일 |
| `collision` | **가구의 "발치(footprint)"만** — 가구가 바닥과 닿는 아랫부분 | 🔴 변경: v1은 가구 전체를 덮었음 → 그러면 가구 뒤에 설 수 없어 R2가 불가능. **가구 밑변 주변 얇은 띠만** 막는다 (벽은 기존대로 전체) |
| `overhead` 🆕 | **가구의 "상단부"** — 캐릭터를 가려야 하는 윗부분. 타이트함 3단계: ① 사각형(윤곽이 네모난 가구) ② **폴리곤**(비스듬한 윤곽) ③ **픽셀 마스크**(케이블·틈새 있는 복잡한 가구 — `make-occluder-mask.py`로 누끼 PNG 생성 후 사각형 오브젝트에 커스텀 속성 `mask=<파일명>`) | 신규. 프론트가 이 영역을 배경에서 잘라 캐릭터 위에 y-정렬로 겹침. 밑변 = 가구가 바닥에 닿는 선 (앞/뒤 판정 기준). ⚠️ **영역에 '그냥 길' 픽셀이 들어가면 캐릭터가 길에서 잘려 보이는 오탐 발생** — PoC에서 확인: 폴리곤도 틈새 바닥은 1~2px 물 수 있어, 모니터 책상 같은 복잡 가구는 마스크가 정답 (팀장 지적 반영) |
| `spawns` | `player` + NPC 자리 | 자리 수 = **시나리오별 표**(태수 제공) 따름. 이름: 기존 3종(teamjang/sasu/bujang) + 초과분은 `npc4`, `npc5` (백엔드 확장 예정) |

**한 가구 = collision(발치 띠) + overhead(상단부)** 세트로 그린다. 예: 책상 —
```
┌─────────────┐  ← overhead (책상 전체 상판·다리 위쪽) : 캐릭터를 가림
│   desk      │
╞═════════════╡  ← 이 경계선 = overhead 밑변 = 앞/뒤 판정 기준
│▒▒collision▒▒│  ← 발치 띠(높이 1칸 정도) : 통과만 막음
└─────────────┘
```
→ 캐릭터가 책상 **위쪽(뒤)**에 서면 상반신만 보이고, **아래쪽(앞)**에 서면 전신이 보임.

**규격 수치:**
- 그리드: 80×45, 24px (기존 유지) · 캔버스 1920×1080
- 통로·문: 최소 2칸, 권장 3칸
- spawn `player`: 맵 입구(주 출입구 안쪽) — 시작하자마자 사방이 열린 곳

---

## 4. 수용 기준 (맵 1장 완성 시 체크)

- [ ] **이미지 단계**: §2 프롬프트 조항 충족 — 모든 방에 열린 출입구, 계단·단차 없음, 시점 일관 (불합격 시 좌표 작업 전에 재생성)
- [ ] **도달성**: playtest.html에서 플레이어로 **모든 방에 실제로 들어가짐** (막힌 구역 0)
- [ ] **오클루전**: 책상 뒤에 서면 하반신이 가려지고, 앞에 서면 전신이 보임
- [ ] **오탐 클리핑 없음**: 가구 주변을 한 바퀴 돌며 — **그냥 길인데 몸이 잘려 보이는 지점 0** (overhead가 바닥 픽셀을 물고 있으면 발생, 윤곽 다시 따기)
- [ ] **NPC**: 시나리오별 자리 수 충족, 각 자리에 도달 가능(반경 130px 이내 접근)
- [ ] **통로**: 좁은 길목도 발판 46px 통과 (playtest에서 걸림 없음)

## 5. 역할 분담

| 몫 | 담당 |
|---|---|
| 배경 이미지 생성 (§2 프롬프트) | 콘텐츠팀 |
| Tiled 좌표 작업 (§3 규격) | 태수 |
| 컨버터 overhead 지원(**사각형+폴리곤+픽셀 마스크**) + 프론트 y-정렬 오클루전 렌더(clip-path/CSS mask, 키 큰 가구 뒤 실루엣 표시 포함) + spawn 자리 확장(npc4·npc5) | **태수 (코드)** — 컨버터·playtest ✅, spawn npc4·npc5 백엔드 ✅(맵에 찍기만 하면 배정됨), 프론트 본편 이식 남음 |
| 시나리오별 NPC 자리 수 표 | **태수** — 미션 등장 NPC 기준 추출 |
| 수용 기준 최종 확인 | 팀장 |

## 부록 — 시나리오별 NPC 자리 수 (미션+돌발퀘 등장 기준, 태수 추출)

| 자리 수 | 시나리오 |
|---|---|
| **1자리** | ys-08 |
| **2자리** | jm-04 · stn-01 · yg-03 · ys-03 · ys-04 · ys-06 · ys-09 |
| **3자리** | backend-dev-day1 · cln-01 · gm-01 · hr-01 · jm-03 · kts-01 · kts-02 · kts-03 · ms-09 · sns-01 · stn-03 · stn-04 · stn-05 · wh-01 · yg-01 · ys-05 · ys-07 |
| **4자리** | jm-02 · jm-05 · kts-04 · kts-05 · ms-02 · ms-03 · ms-04 · ms-05 · ms-07 · stn-02 · yg-02 · yg-04 · yg-05 · ys-01 · ys-02 · ys-10 |
| **5자리** | jm-01 · ms-01 · ms-06 · ms-08 · ms-10 |

> 자리 이름: 1~3번째 = teamjang·sasu·bujang, 4번째부터 npc4·npc5. 총 46개 시나리오.
