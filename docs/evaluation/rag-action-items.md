# RAG 조치 항목 정리 — 무엇을 왜 고치는가

작성 2026-07-28 · 김태수
**갱신 2026-07-28 — 5번을 실측으로 재확인해 종결했습니다. 아래 「갱신 요약」부터 보세요.**

평가지표 보고서의 조치 제안 중 RAG 관련 5건을 코드로 확인한 결과입니다.

---

## 갱신 요약

| # | 항목 | 상태 |
|---|---|---|
| 1 | `scope_chunks` 최빈 판정 | **완료** · 머지됨 |
| 2 | `max_distance` 0.31 | **완료** · 머지됨 |
| 3 | 임베딩 타임아웃 | **종결** · 원인은 keepalive였고 이미 고쳐졌습니다 |
| 4 | 무근거 시 침묵 | **완료** · 머지됨 (`knowledge=None` 강제 검증 0/6) |
| 5 | 같은 F 내 재순위화 | **종결 — 하지 않습니다** |
| 6 | (신규) 단계 오답 | **완료** · `RAG_TOP_K` 3 → 5 |

### 5번을 왜 닫았는가 — 채점 기준이 틀렸습니다

5번의 근거는 "F 스코프로 좁혔는데도 직무 오염 35.4%"였습니다. 그 35.4%는
**정답을 청크 1개(`gt_chunk_id`)로 잡은 채점**에서 나온 값입니다.

코퍼스를 까보니 이렇습니다.

```
J 청크 515개 = 직무 103개 × 단계 5개
머리말 [직무명/직무군/유형]을 떼면 고유 본문은 120개뿐 (= 직무군 24 × 단계 5)
515개 중 490개(95.1%)가 같은 직무군 안에서 본문이 글자 단위로 동일
```

임베딩은 본문으로 거리를 잽니다. **본문이 같으면 어느 직무가 뽑히든 거리가 같습니다.**
그런데 정답을 하나만 인정하니, 글자 단위로 똑같은 근거가 검색돼도 "오염"으로 셌습니다.

정답을 **동등 청크 집합**(본문이 같은 청크 전부)으로 바꿔 다시 재면 이렇습니다.

| | 옛 채점 | 새 채점 |
|---|---:|---:|
| Recall@1 | 67.5% | **80.0%** |
| 직무 오염 (top-1) | 12.5% | **0.0%** |
| 직무군 오염 (top-1) | — | **0.0%** |

재순위화가 잡으려던 대상이 존재하지 않습니다.

### 마지막 구멍도 확인했습니다

"본문이 같으면 동등하다"에는 빈틈이 하나 있었습니다. 머리말 `[직무명/직무군/유형]`은
본문과 함께 프롬프트에 들어가므로, 본문이 같아도 머리말이 다른 직무를 가리키면
LLM이 엉뚱한 직무명을 말할 수 있습니다 — 5번이 든 "호텔리어 질문에 승무원 지식"이
정확히 이 경우입니다.

재봤습니다. 머리말 불일치 6건(15.0%)인데 **전부 질문이 직무를 특정하지 않은 문항**입니다.
직무를 콕 집어 물은 20건에서는 **불일치 0건**입니다.

```
J004 문서관리 담당      ← 머리말 J003     J028 시장조사 보조      ← 머리말 J027
J005 비품·자산관리 보조  ← 머리말 J003     J036 프로젝트관리 보조   ← 머리말 J034
J018 재고관리 사무      ← 머리말 J015     J097 통신장비 설치 보조  ← 머리말 J094
```

사용자가 직무를 묻지 않았는데 같은 직무군의 다른 직무 라벨이 붙는 건 오답이 아니고,
물었을 때는 정확히 그 직무가 나옵니다.

### 이 문서의 앞 판이 틀렸던 지점

골든셋(`298daa0`, 영수님)은 문항마다 `gt_chunk_id` 하나를 정답으로 둡니다. 코퍼스에
본문 중복이 95%라는 걸 모르면 자연스러운 설계입니다.

**제 잘못은 그 수치를 검증 없이 문서에 옮겨 "재순위화가 필요하다"는 결론까지 낸 것입니다.**
숫자를 만든 쪽이 아니라 숫자를 그대로 믿은 쪽의 문제입니다. 앞으로 이 문서에 오는
측정치는 채점 기준까지 확인하고 싣겠습니다.

### 재현 방법

```bash
docker compose exec api python -m app.scripts.reclassify_golden          # 분류 재생성
docker compose exec api python -m app.scripts.eval_retrieval             # 옛/새 채점 비교
docker compose exec api python -m app.scripts.eval_retrieval --mode prod # 상담 실경로
```

---

## 0. `max_distance` 반영 — 해결됨

`dev`에 0.35로 남아 있던 값은 0.31로 머지됐습니다. 두 도메인(consultation·simulation)
모두 적용됐고, `test_consultation_and_simulation_thresholds_in_sync`가 동기화를 강제합니다.

---

## 1. `scope_chunks` — 1등만 보고 나머지를 버린다 · 완료

### 무엇이 문제였나

상담 RAG는 후보를 6개 뽑은 뒤 "이게 어느 직무 얘기인지" 판정해 좁힙니다. 그 판정이
1등 청크의 직무만 봤습니다.

```
후보: [J021, J058, J060, J058, J058, J059]
       ↑1등

정답 J058이 6개 중 3개로 최다인데,
1등이 J021이라 same=[J021] 1개 → 조건 미달 → 6개 전부 폐기
```

지식이 있는데도 한 건도 주입되지 않았습니다.

### 조치

판정을 **1등 → 최빈**으로 바꾸고, 동률은 **거리 합이 작은 쪽**으로 정했습니다
(팀 합의 2026-07-28). 후보 6개 기준 최빈=정답이 15/18이었습니다.

---

## 2. `max_distance` 0.35 → 0.31 · 완료

| 컷 | Recall@5 | 오프토픽 오통과 |
|---|---:|---:|
| 0.30 | 1.000 | **0%** |
| 0.35 (옛값) | 1.000 | **0%** |
| 0.40 | 1.000 | 20% |
| 0.45 | 1.000 | 75% |

컷은 **in-domain 질의에는 무력**합니다 — 상위 200청크 거리 최댓값이 0.293이라 0.30
컷조차 아무것도 못 자릅니다. 실제 역할은 **오프토픽 거절**입니다.

- in-domain 1위 max **0.267** / 오프토픽 1위 min **0.372** → 겹침 0, 안전밴드 **[0.267, 0.372]**
- 0.35는 밴드 상단 치우침(거절 여유 0.022) → 0.31이면 거절 0.062 / 회수 0.043으로 균형
- Recall 손실 0

`test_cut_stays_inside_measured_safe_band`가 밴드 경계를 고정합니다.

---

## 3. 임베딩 타임아웃 — 종결. 원인은 keepalive였고 이미 고쳐졌습니다

현재 값은 **`settings.rag_embed_timeout_s = 2.0`** (`.env`로 조정 가능)입니다.

### 진단이 틀렸습니다 — "캐싱·재시도"가 아니라 커넥션이었습니다

앞 판은 지연의 원인을 "Gemini API 자체의 꼬리"로 보고 **캐싱이나 재시도**를 제안했습니다.
실제 원인은 **httpx `keepalive_expiry`가 5초**라 상담 턴 간격(20~40초)마다 TLS를
재수립한 것이었습니다. 300초로 늘리자 사라졌습니다.

### 재측정 (2026-07-29, keepalive 300s 적용 후)

**연속 호출 40회**

| | 이전(장애 당시) | 지금 |
|---|---:|---:|
| p50 | 410ms | **347ms** |
| p90 | 1,302ms | **416ms** |
| max | 10,190ms | 1,359ms |
| 2.0s 초과 | 7.5% | **0/40 (0%)** |

**턴 간격을 둔 호출** — 이게 실제 상담 조건입니다.

| 간격 | 실측 (3회) |
|---|---|
| 3초 | 366 · 414 · 502ms |
| 10초 | 381 · 366 · 355ms |
| 30초 | 355 · **10,079** · 3,431ms |
| 45초 | 378 · 364 · 395ms |

**간격은 더 이상 원인이 아닙니다.** 45초에서 정상인데 30초에서 튀었으니 커넥션 만료가
아니라 **무작위 꼬리**입니다. keepalive가 걷어낸 것은 *체계적인* +885ms였고, 이건 다른
현상입니다.

### 남은 꼬리 — 대응하지 않습니다

52회 중 2회가 2.0s를 넘었습니다(약 4%). 제안됐던 두 방법을 실측으로 검토했습니다.

**캐싱** — 상담 발화는 자유 텍스트라 재사용이 거의 없습니다.

```
RAG 대상 고유 발화 207건
  2명 이상이 물은 발화     25건 (12.1%)   ← 캐시가 실제로 먹힐 상한
  한 사람만 물은 발화     182건           ← 같은 사람의 테스트 반복
```

전체 반복률은 50.4%로 보이지만 **대부분 개발자가 같은 질문을 24~28회 넣은 것**입니다.
실사용에서 기대할 수 있는 적중은 **12% 남짓**이고, 그마저 꼬리를 겨냥하지 못합니다
(꼬리는 무작위라 캐시 미스 쪽에도 똑같이 옵니다).

**재시도** — 이미 느린 호출을 한 번 더 하면 지연이 늘어납니다. 첫 토큰 앞 직렬 구간이라
사용자가 그 시간을 그대로 기다립니다.

**지금 설계가 이 꼬리에 대한 올바른 대응입니다.** 2.0s에서 끊고 지식 없이 진행하는 것이
"짧은 지연 > 지식 주입"이라는 원래 정책과 일치합니다. 손실은 `[RAG-EMBED]` 로그로
관측되며 `log_report.py`가 집계합니다.

### 남은 확인 하나

위 수치는 **개발 환경**입니다. 운영에서도 같은지는 VM 로그로 확인합니다.

```bash
docker compose -f infra/docker-compose.yml logs --since 30m api | python3 scripts/log_report.py
```

타임아웃 비율이 유의미하게 높으면 `.env`의 `RAG_EMBED_TIMEOUT_S`를 그 환경 p95 위로
올립니다 — **코드 배포 없이 됩니다.**

---

## 4. 무근거 시 침묵 — 완료

앞 판의 진단은 이랬습니다.

```
임베딩 타임아웃 (3번)  ─┐
                        ├─▶ RAG 미주입 25% ─▶ 근거 없는 단정 15%
scope_chunks 오탈락(1번)─┘
```

### 양쪽 다 조치됐습니다

**증상 쪽** — 영수님이 `edb34bc`에서 상담 프롬프트를 조이고, `knowledge=None`을
강제해(= RAG가 아예 안 들어온 최악의 경우) 6문항으로 검증했습니다.

> 지어낸 단정 **0/6**. 연봉·경쟁률·자격증 기간은 전부 "자료로는 확인 어렵다"로
> 회피했고, 부당해고 질문은 3회 반복 모두 법적 판단을 거부하고 노무사·노동청으로
> 넘겼습니다.

무근거 상황을 인위적으로 만들어 잰 값이라, 미주입률이 얼마든 이 동작은 유지됩니다.

**원인 쪽** — 1번이 머지되고 주입 상한도 올라가면서 미주입률 자체가 떨어졌습니다.
상담 실경로를 그대로 태워 잰 값입니다(임베딩 타임아웃 제외).

| 스코프 | 지식 주입됨 | 정답 근거 포함 |
|---|---:|---:|
| 추천 적중 | 100.0% | **100.0%** |
| 추천 전(전역) | 95.0% | 95.0% |

남은 미주입은 전역 검색에서 `지배 직무 없음`으로 걸러진 2건(5%)뿐이고, 여기에
임베딩 타임아웃 10%가 별도로 얹힙니다.

증상과 원인이 각각 다른 방법으로 확인됐으므로 **추가 조치는 없습니다.** 앞 판의
'근거 없는 단정 15%'는 조치 전 수치이니 인용하지 마세요.

---

## 5. 같은 F 내 재순위화 — 종결, 하지 않습니다

### 앞 판에 적었던 근거

| 조건 | Recall@1 | 직무군 오염 | 직무 오염 |
|---|---:|---:|---:|
| 전역 | 65.0% | 7.9% | 39.2% |
| F 스코프 | 65.0% | **0.0%** | **35.4%** |

"직무군 오염은 스코프로 0%가 됐는데 직무 오염은 거의 안 줄었다 → 같은 직무군 안
인접 직무끼리 섞인다 → 스코프 뒤에 재순위화가 필요하다"는 흐름이었습니다.

### 왜 틀렸나

35.4%는 **채점 기준 탓 허수**입니다. 상세는 위 「갱신 요약」에 적었습니다. 요약하면
같은 직무군 안 청크들이 본문이 글자 단위로 같아서, 어느 게 뽑혀도 근거가 동일한데
정답을 하나만 인정해 오염으로 셌습니다.

동등 청크를 인정하고 다시 재면 직무 오염이 **0%**입니다. 순수 검색(raw)과 상담
실경로(prod) 양쪽에서 같습니다. 머리말 직무명까지 따져도 **직무를 특정한 질문에서는
불일치 0건**입니다.

### 덧붙여 — 청크 통합과 질문 라우팅도 같이 무너집니다

전달받은 심층 분석에서 이어진 후속안 두 가지도 전제가 같습니다.

| 후속안 | 전제 | 실측 |
|---|---|---|
| J 공통 청크 → F 청크 통합 | 같은 F 안 직무 오염이 크다 | 오염 0% |
| 질문 라우팅 (F / J / 세부직업) | 스코프가 맞아도 직무를 못 고른다 | 추천 적중 시 주입·정답 100% |

특히 **청크 통합은 손해입니다.** 통합은 머리말의 직무명을 없애는 방향인데, 지금
직무를 정확히 맞히게 해주는 신호가 바로 그 머리말입니다.

---

## 6. (신규) 단계 오답 — `RAG_TOP_K` 3 → 5 · 완료

직무 오염이 0인데도 실패가 남아 원인을 봤더니 **단계**였습니다.

`scope_chunks`는 결과를 단일 직무로 붕괴시킵니다. 그런데 그 직무의 청크는 단계 5개
(업무요청 이해 / 자료·현황 확인 / 처리·제작·응대 / 검수·판단 / 보고·인계)로 나뉩니다.
3개만 주입하면 정답 단계가 밀려 빠집니다.

```text
J012 발주관리 담당  정답 처리·제작·응대  주입 [검수·판단, 보고·인계, 업무요청 이해]
J020 채용운영 보조  정답 검수·판단      주입 [보고·인계, 업무요청 이해, 처리·제작·응대]
```

정답 근거 포함률입니다.

| 스코프 | 주입 3 | 주입 5 |
|---|---:|---:|
| 추천 적중 | 95.0% | **100.0%** |
| 추천 전(전역) | 90.0% | 95.0% |

후보 수를 6 → 10으로 넓히는 건 양쪽 다 효과가 없었습니다. 병목은 후보가 아니라
주입 수입니다. 청크가 ~215자라 5개여도 ~1075자입니다.

**시뮬레이션 NPC의 `RAG_TOP_K = 3`은 그대로 뒀습니다.** NPC는 아는 만큼만 말해야 하고,
이 골든셋은 상담 질문으로만 이뤄져 NPC 쪽 근거가 되지 못합니다.

---

## 정리

| # | 항목 | 상태 | 남은 일 |
|---|---|---|---|
| 1 | `scope_chunks` 최빈 판정 | 완료 | — |
| 2 | `max_distance` 0.31 | 완료 | — |
| 3 | 임베딩 타임아웃 | **종결** | — (캐싱·재시도는 실측으로 불필요 판정) |
| 4 | 무근거 시 침묵 | 완료 | — |
| 5 | 재순위화 | **종결** | 하지 않음 |
| 6 | 단계 오답 | 완료 | — |

**여섯 항목 모두 닫혔습니다.** 마지막 3번은 재측정 결과 원인이 keepalive였고 이미
고쳐진 상태라, 제안됐던 캐싱·재시도는 실측으로 불필요하다고 판단했습니다(§3).

남은 것은 **조치가 아니라 확인** 하나입니다 — 운영 환경 `[RAG-EMBED]` 분포가 개발
환경과 같은지 보는 일이며, 아래 한 줄이면 됩니다.

```bash
docker compose -f infra/docker-compose.yml logs --since 30m api | python3 scripts/log_report.py
```

타임아웃 비율이 높게 나오면 `.env`의 `RAG_EMBED_TIMEOUT_S`만 올리면 됩니다(코드 배포 불필요).
